comment:
  layout: header, diff, tree, changes
coverage:
  status:
    patch:
      default:
        branches: null
        target: 90.0
        threshold: 10.0
    project:
      default:
        branches: null
        target: 80.0
        threshold: 100.0
  ignore:
  - "go/vendor/*"
